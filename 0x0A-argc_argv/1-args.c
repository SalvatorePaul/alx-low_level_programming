#include <stdlib.h>
#include <stdio.h>

/**
 * main - a program that prints the number of arguments passed into it.
 * @argc: argument count
 * @
 * Return: exit_success
 */

int main(int argc)
{
	printf("%d\n", argc - 1);
	return (0);
}
