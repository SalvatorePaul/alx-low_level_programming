#include "main.h"
#include <stdio.h>

/**
 * int _abs - a function that computes the absolute value of an integer
 * @c: the integer to check
 * Return: the anolute value of an integer
 */

int _abs(int abs)
{
	 if ( abs >= 0)
	return (abs);
	 else
	return (-abs);
}
